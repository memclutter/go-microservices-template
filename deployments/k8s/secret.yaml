apiVersion: v1
kind: Secret
metadata:
  name: api-secret
  namespace: microservices
type: Opaque
stringData:
  DB_USER: "postgres"
  DB_PASSWORD: "changeme"  # Use external secret management in production
  RABBITMQ_USER: "guest"
  RABBITMQ_PASSWORD: "guest"
